language: java

install:
  - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V

script:
  - mvn -e clean test -B


jdk:
  - oraclejdk8
  - openjdk8
  
os:
  - linux

matrix:
  fast_finish: true